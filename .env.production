# Build production (ex.: Vercel): base vazia = mesma origem HTTPS; vercel.json faz proxy ao backend.
# Sobrescreve VITE_* do .env — evita Mixed Content (HTTPS → http://... bloqueado no browser).
VITE_API_URL=
VITE_SERVER_URL=
